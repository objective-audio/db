language: objective-c
osx_image: xcode10
branches:
  only:
    - master
notifications:
  email: false
install:
  - brew install clang-format
script:
  - xcodebuild -workspace db_mac.xcworkspace -scheme db_mac_tests -configuration Debug OBJROOT=build -sdk macosx clean test
